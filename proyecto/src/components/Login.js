

var username = document.querySelector('#username') // dos formas de seleccionar un elemento de html

var password = document.querySelector('#password')
var licensePlate = document.querySelector('#licensePlate')

// botón de Login
var loginButton = reactDom.render(document.getElementById('login')) // estoy buscando el botton de login
var checkinButton = reactDom.render(document.getElementById('checkin'))
var recoverPassword = ReactDOM.render(document.getElementById('recoverpassword'))

 loginButton.addEventListener('click', () => {

}) // recive un evento y una función

checkinButton.addEventListener('click', () => {
    return 
})

recoverPassword.addEventListener('click', () => {
    return 
}) 